node_modules/
dist/
*.tsbuildinfo
.DS_Store
*.local

# Never commit the source PDFs — they are inputs, not site content.
*.pdf

# ...but the generated JD PDFs are deliverables we share with colleges.
!jd/pdf/*.pdf
